
		<% if @task.errors.any? %>
			<div class="error_explanation">
			  <h2><%= pluralize(@task.errors.count, "error") %> prevented this record from being saved:</h2>
			  <ul>
			  <% @task.errors.full_messages.each do |msg| %>
			    <li><%= msg %></li>
			  <% end %>
			  </ul>
			</div>
		<% end %>